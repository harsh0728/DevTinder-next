export interface LoginFormData {
  emailId: string;
  password: string;
}

export interface SignupFormData {
  firstName: string;
  lastName: string;
  emailId: string;
  age:number;
  gender:string
}